/// <reference types="vite/client" />

interface ImportMetaEnv {
  readonly VITE_TRACESHIELD_CORE_BASE_URL?: string;
  readonly VITE_USE_MOCK_DATA?: string;
}

interface ImportMetaEnv {
  readonly VITE_TRACESHIELD_CORE_BASE_URL?: string;
  readonly VITE_USE_MOCK_DATA?: string;
}

interface ImportMeta {
  readonly env: ImportMetaEnv;
}
